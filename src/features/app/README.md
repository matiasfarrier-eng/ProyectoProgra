Feature `app`

Contiene la composición del shell de la aplicación:
- `AppRoutes` para la orquestación de módulos/feature pages
- `AppProviders` para providers globales como router y React Query

Este feature no expone UI reutilizable; es la capa de arranque del producto.
